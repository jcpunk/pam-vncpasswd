/**
 * autoconf.h - CMake-generated configuration header
 *
 * DO NOT EDIT
 * This file is automatically generated by CMake from autoconf.h.in
 */

#ifndef PAM_FNAL_VNCPASSWD_AUTOCONF_H
#define PAM_FNAL_VNCPASSWD_AUTOCONF_H

/* project/program name */
#define PROJECT_NAME "@PROJECT_NAME@"

/* Version information */
#define VERSION "@VERSION@"

/* Path to system login.defs file */
#define LOGIN_DEFS_PATH "@LOGIN_DEFS_PATH@"

/* Default encryption method if login.defs does not specify one */
#define DEFAULT_ENCRYPT_METHOD "@DEFAULT_ENCRYPT_METHOD@"

/* Default yescrypt cost factor if login.defs does not specify one */
#define DEFAULT_YESCRYPT_COST @DEFAULT_YESCRYPT_COST@

/* Default SHA-crypt rounds if login.defs does not specify */
#define DEFAULT_SHA_CRYPT_ROUNDS @DEFAULT_SHA_CRYPT_ROUNDS@

/* VNC password subdirectory name under user home */
#define VNC_PASSWD_DIR "@VNC_PASSWD_DIR@"

/* VNC password filename within VNC_PASSWD_DIR */
#define VNC_PASSWD_FILE "@VNC_PASSWD_FILE@"

/*
 * Maximum password length enforced by fnal-vncpasswd.
 * The VNC protocol (RFB) limits VNC Authentication passwords to 8 characters.
 * Passwords longer than this are rejected at set-time to avoid silent
 * truncation and interoperability issues with VNC clients.
 */
#define MAX_PASSWORD_LENGTH @MAX_PASSWORD_LENGTH@

/* Minimum password length enforced by fnal-vncpasswd */
#define MIN_PASSWORD_LENGTH @MIN_PASSWORD_LENGTH@

#endif /* PAM_FNAL_VNCPASSWD_AUTOCONF_H */
